class WrapperClass1
{
    public static void main(String[]args)
    {
        int a=20;
        Integer i=Integer.valueOf(a);//Converting int to Integer explicitly
        Integer j=a;//AutoBoxing,now compiler will  write Integer.valueOf internally
        System.out.println(a+" "+i+" "+j);
    }
}
